{
  "name": "tailwind-css",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",

    "comment1": "below code Runs Tailwind compiler in watch mode",
    "dev": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/output.css --watch",

    "comment2": " below code Minifies CSS for production/live site",
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/output.css --minify"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "tailwindcss": "^4.2.1"
  }
}
